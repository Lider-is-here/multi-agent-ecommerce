# Multi-Agent 电商智能服务平台

> 核心开发人员 | 大模型应用 / 多智能体协作系统

***

## 一、项目概述

### 1.1 项目背景与定位

传统电商客服系统存在三大痛点：意图识别单一（难以处理跨域复杂请求）、响应延迟高（人工介入环节多）、个性化程度低（千人一面）。本项目基于 Multi-Agent 架构构建电商智能服务平台，通过专业智能体分工协作，实现从商品发现、订单管理到售后履约的全链路智能化服务。

### 1.2 核心指标

| 维度         | 数据                         |
| ---------- | -------------------------- |
| 专业智能体数量    | 4 个独立部署                    |
| 领域工具函数总数   | 30+                        |
| 单次请求最大协作轮次 | 20 轮编排                     |
| 兼容 LLM 提供商 | 3 家（OpenAI / Azure / 通义千问） |
| 语义向量检索维度   | 1536 维                     |
| 容器化部署      | Docker Compose 一键启动        |

***

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│               Next.js 16 前端层                          │
│  商品浏览 / 智能对话 / 订单管理 / 管理后台 / 卖家中心      │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTPS / SSE 流式
┌──────────────────────▼──────────────────────────────────┐
│              Orchestrator 编排器智能体                    │
│   意图识别路由   │   A2A 协议封装   │   流式响应聚合       │
└───┬───────────┬───────────┬───────────────┬─────────────┘
    │ A2A/HTTP  │ A2A/HTTP  │  A2A/HTTP     │  A2A/HTTP
┌───▼────┐ ┌────▼─────┐ ┌────▼──────┐ ┌─────▼──────────┐
│ 产品发现 │ │ 订单管理  │ │ 定价促销   │ │ 库存与履约     │
│ 智能体   │ │ 智能体    │ │ 智能体     │ │ 智能体         │
└───┬─────┘ └────┬─────┘ └────┬──────┘ └─────┬──────────┘
    │            │             │              │
    └────────────┴──────┬──────┴──────────────┘
                       │ 共享工具层
              ┌────────▼──────────┐
              │  共享基础设施层    │
              │  PostgreSQL+pgvector │  Redis  │
              │  OTEL 可观测 + Langfuse 追踪 │
              │  JWT 本地 + OAuth2 双模式认证 │
              └───────────────────┘
```

### 2.2 架构设计理念‘’ 

1. **职责分离**：每个专业智能体只聚焦单一业务域，通过编排器统一调度，避免"全能智能体"能力稀释。
2. **独立部署扩展**：4 个专业智能体均为独立 FastAPI 服务，可独立扩缩容、独立升级，互不影响。
3. **通信标准化**：智能体间通信遵循 A2A（Agent-to-Agent）规范，统一认证、序列化、追踪格式。
4. **流式优先**：全链路支持 SSE 流式输出，降低首字延迟，提升交互体验。

***

## 三、核心技术深度

### 3.1 LangGraph 智能体引擎

基于 LangGraph 搭建通用 ReAct 智能体框架，统一封装 5 个智能体的构建流程：

- **状态定义**：每个智能体维护三类状态——对话历史（带消息累加器）、用户与会话元数据、执行步骤时间线。
- **工作流编排**：使用 LangGraph StateGraph 构建"LLM 推理节点 ↔ 工具执行节点"的条件循环，由路由函数动态判断是否继续调用工具或结束。
- **多 LLM 兼容**：针对通义千问等模型工具调用返回结构缺字段的问题，在推理节点输出后增加兼容补丁层，自动补全缺失字段，保证 LangGraph ToolNode 正常执行。
- **异步执行**：工具节点全程异步调用，适配高并发 I/O 密集场景（数据库查询、HTTP 调用）。
- **迭代收敛控制**：每个智能体独立配置最大迭代次数，防止 LLM 陷入工具调用死循环。

### 3.2 A2A 智能体间通信

实现跨智能体可靠通信方案：

| 层级   | 实现方案                                                          |
| ---- | ------------------------------------------------------------- |
| 传输层  | HTTP/1.1 + JSON，双模式：同步 message:send / 流式 message:stream       |
| 认证层  | 双模式切换：① Local 模式使用共享密钥请求头；② OAuth2 模式使用 RS256 JWT + JWKS 公钥验证 |
| 身份透传 | 三头发送：用户邮箱 / 用户角色 / 会话 ID，全链路用户身份不丢失                           |
| 追踪层  | OpenTelemetry Span 包装，支持分布式调用链可视化                             |
| 降级策略 | 流式失败自动降级为同步调用；超时/错误统一封装用户友好提示                                 |

流式调用时，编排器作为 SSE 中继逐行转发专业智能体的 data/event 块到前端，同时累加 step 事件到步骤时间线。

### 3.3 向量语义检索

基于 PostgreSQL 16 + pgvector 实现商品语义检索：

- **向量存储**：独立商品向量表，每条商品记录对应 1536 维 Embedding 向量；使用 IVFFlat 余弦相似度索引加速 Top-K 召回。
- **检索流程**：用户自然语言 → Embedding 向量化 → pgvector 余弦相似度召回 → 库存/价格过滤 → 排序输出。
- **检索场景**：支持语义搜索（口语化模糊查询）、相似商品推荐（基于向量的"猜你喜欢"）、趋势商品（结合销量热度的综合排序）三类模式。

### 3.4 双模式认证体系

支持 Local/OAuth 双模式一键切换（环境变量控制）：

- **Local 模式**：HS256 JWT + 共享密钥，开箱即用，适用于开发/轻量部署。
- **OAuth 模式**：自托管授权服务器 + RS256 非对称签名 + JWKS 公钥分发，适用于生产级部署。
  - 用户登录：password 授权流程，Access Token 1 小时 + Refresh Token 7 天。
  - 服务间调用：client\_credentials 授权流程，A2A 场景强制身份可追溯。
  - 可选支持 RFC 7591 动态客户端注册。
- **安全启动校验**：Pydantic 配置校验器在启动时强制检查密钥强度——生产环境拒绝占位符密钥/弱密钥（<256 位），开发环境 WARNING 提示。

### 3.5 人机协作（HITL）审批机制

针对高风险操作引入审批队列：

- **触发范围**：取消订单、处理退款、发起退货、修改订单、缺货下单这 5 类操作。
- **阈值策略**：退货/退款金额超过 500 美元强制进入审批；低于阈值可配置自动放行。
- **审批界面**：管理后台 /admin/approvals 提供审批列表与操作界面。
- **持久化队列**：审批请求存储于数据库队列表，支持超时重试与全链路审计。

***

## 四、专业智能体详细设计

### 4.1 产品发现智能体（端口 8081，温度 0.7，最大迭代 15）

| 工具分类 | 能力覆盖                         |
| ---- | ---------------------------- |
| 搜索类  | 关键词多条件筛选、向量语义搜索、相似商品推荐、趋势商品榜 |
| 详情类  | 商品详情与规格查询、多商品横向对比（价格/规格/库存）  |
| 库存类  | 实时库存查询、多仓分布查询                |
| 价格类  | 价格趋势与历史最低价分析                 |
| 用户类  | 用户画像读取、购买历史个性化匹配             |
| 记忆类  | 跨会话用户偏好存储与召回                 |

### 4.2 订单管理智能体（端口 8082，温度 0.2，最大迭代 15）

| 工具分类 | 能力覆盖                    |
| ---- | ----------------------- |
| 订单查询 | 订单列表（状态筛选）、订单详情（含商品明细）  |
| 订单追踪 | 物流状态时间线（含节点地理位置）        |
| 订单操作 | 取消订单 / 修改订单（走 HITL 审批）  |
| 退货退款 | 退货资格校验、发起退货、处理退款、退货状态追踪 |
| 购物车  | 增删改查、商品数量调整             |
| 地址管理 | 收货地址 / 账单地址设置、一键同址      |

### 4.3 定价促销智能体（端口 8083，温度 0.7，最大迭代 15）

- 当前生效促销活动查询（满减/折扣/赠品，含截止时间提示）
- 商品价格波动分析（辅助判断优惠真实性）
- 结合用户画像与购买历史做分层优惠推荐

### 4.4 库存与履约智能体（端口 8085，温度 0.2，最大迭代 15）

| 工具分类 | 能力覆盖                 |
| ---- | -------------------- |
| 库存管理 | 实时库存、多仓分布、补货计划查询     |
| 物流估价 | 运费估算、多承运商比价（时效/成本权衡） |
| 履约追踪 | 物流追踪、智能分仓发货路径规划      |
| 缺货处理 | 缺货登记下单（走 HITL 审批）    |

***

## 五、工程化实践

### 5.1 配置管理

基于 Pydantic v2 Settings 的全局配置中心：

- **集中声明**：所有环境变量在唯一配置类中声明，下游模块禁止直接读 os.environ。
- **双层加载**：项目根 .env 文件优先，Docker 环境变量覆盖，适配本地/容器双场景。
- **多 LLM 别名兼容**：OPENAI\_API\_KEY / DASHSCOPE\_API\_KEY 互别名；AZURE\_OPENAI\_KEY / AZURE\_OPENAI\_API\_KEY 互别名，切换模型供应商零代码改动。
- **安全校验**：生产环境强制检查密钥强度，启动即失败，杜绝弱配置上线。

### 5.2 容器化部署

Docker Compose 全链路一键启动：

| 服务                    | 端口         | 说明                             |
| --------------------- | ---------- | ------------------------------ |
| PostgreSQL + pgvector | 15432:5432 | 含健康检查，首次启动自动执行 init.sql        |
| Redis 7               | 6379:6379  | 缓存与会话存储                        |
| 编排器智能体                | 8080:8080  | 系统入口                           |
| 产品发现智能体               | 8081:8081  | 独立服务                           |
| 订单管理智能体               | 8082:8082  | 独立服务                           |
| 定价促销智能体               | 8083:8083  | 独立服务                           |
| 库存履约智能体               | 8085:8085  | 独立服务                           |
| 数据库种子                 | —          | profiles: \["seed"]，初始化测试数据后退出 |
| MCP 产品/库存服务           | 9000/9001  | profiles: \["mcp"]，可选数据层抽象     |
| Next.js 前端            | 3000:3000  | profiles: \["frontend"]，可选     |

使用 YAML 锚点统一 5 个智能体服务的环境变量，避免重复配置。

### 5.3 前端工程

技术栈：Next.js 16 App Router + React 19 + TypeScript 5 + Tailwind CSS 4 + shadcn/ui + Radix UI

页面结构：

- **主应用区**：首页、商品列表/详情、购物车、结算、订单中心/详情、智能对话（SSE 流式）、智能体面板、个人中心、卖家中心、管理后台（审批/审计/用量）
- **店铺入口区**：独立店铺页、内嵌 AI 助手
- **认证区**：登录 / 注册

亮点实现：

- react-markdown + rehype-sanitize 安全渲染 LLM 输出
- framer-motion 过渡动画与交互反馈
- sonner Toast + Radix UI 无障碍交互组件
- recharts 数据可视化（管理后台用量、调用趋势）
- Playwright E2E 测试 + Vitest 单元测试

***

## 六、可观测性与系统保障

### 6.1 三层可观测体系

1. **OpenTelemetry 链路追踪**（可配置开关）
   - 统一 OTLP 端点导出
   - 自动注入 A2A 调用 Span
   - 可选控制 LLM 内容采样粒度
2. **Langfuse 智能体专用观测**（并行可选）
   - LLM 调用成本/延迟统计
   - Prompt 版本管理
   - 用户反馈评分追踪
3. **结构化执行步骤**
   - 每次智能体执行完整记录 LLM 调用 + 工具执行链
   - 前端 /runs 页面可视化完整执行轨迹

### 6.2 流式安全防护

- 单次 SSE 流硬超时 120 秒，防止失控流占用工作线程
- 单流输出缓冲上限 10 MB，超限自动截断并标记，避免异常模型输出撑爆内存

***

## 七、可扩展能力（预留接口）

| 能力        | 状态   | 说明                                       |
| --------- | ---- | ---------------------------------------- |
| MCP 数据层抽象 | 可选启用 | 开启后专业智能体通过 MCP Server 获取数据，解耦业务逻辑与数据访问   |
| MCP 资源认证  | 可选启用 | 开启后 MCP 服务强制 OAuth Scope 校验              |
| 多供应商解耦    | 已实现  | 切换 LLM\_PROVIDER 环境变量即可在三家模型提供商间迁移，代码零改动 |

***

## 八、技术栈总览

| 层级      | 技术选型                                  |
| ------- | ------------------------------------- |
| 大模型框架   | LangGraph + LangChain Core            |
| 后端框架    | FastAPI + Uvicorn (ASGI)              |
| 数据库     | PostgreSQL 16 + pgvector + asyncpg    |
| 缓存      | Redis 7                               |
| 前端框架    | Next.js 16 App Router + React 19      |
| UI 组件   | shadcn/ui + Radix UI + Tailwind CSS 4 |
| 认证安全    | JWT (HS256/RS256) + OAuth2 + bcrypt   |
| 可观测     | OpenTelemetry + Langfuse              |
| 容器化     | Docker + Docker Compose               |
| 配置管理    | Pydantic v2 Settings                  |
| 异步 HTTP | httpx AsyncClient                     |
| 数据校验    | Zod (前端) + Pydantic (后端)              |
| 测试      | Vitest + Playwright                   |
| 向量模型    | text-embedding-3-small (1536 维)       |

***

## 九、个人贡献（核心开发）

1. **架构设计**：设计 Multi-Agent 分层架构，制定 A2A 通信协议规范，明确各智能体职责边界。
2. **智能体引擎**：搭建基于 LangGraph 的通用智能体框架，实现 ReAct 循环、多 LLM 兼容补丁、步骤时间线追踪。
3. **A2A 通信**：完成编排器与专业智能体间双模式（流式/同步）HTTP 通信，含认证透传、降级策略、OTel 埋点。
4. **向量检索**：设计 pgvector 向量库 Schema 与索引方案，落地语义搜索、相似推荐、趋势分析三类检索。
5. **认证体系**：实现 Local/OAuth 双模式切换，完成 JWT 校验、密钥强度校验、OAuth client\_credentials 全流程。
6. **工程化**：编写 Docker Compose 多服务编排、Pydantic 配置中心、数据库初始化与种子脚本。
7. **前端核心**：完成智能对话页 SSE 流式渲染、管理后台审批流页、智能体运行轨迹可视化。

***

## 十、未来优化方向

1. **HITL 升级**：引入 LangGraph Human-in-the-Loop 断点续执行，替代当前审批轮询模式。
2. **向量检索升级**：IVFFlat → HNSW 索引，支撑百万级商品毫秒级召回；增加语义缓存层。
3. **推理成本优化**：大小模型路由（轻量模型做意图/抽取，大模型做复杂推理）+ 语义缓存复用。
4. **多模态能力**：商品图理解（以图搜同款）、订单截图结构化解析。
5. **策略调优**：基于用户反馈数据做 RLHF Prompt 优化与工具选择策略强化学习。

